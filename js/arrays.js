
/* array

    array is ordered collection of simillar type data elements.
    But in js we can store values of different as well.

*/



/* methods that mutate original array.

    push()      add element at end

    pop()       remove last element

    unshif()    add element at begining

    shif()      remove first element

    splice()    add, remove or replace elements

*/

/* methods that return new array.

    map()       iterate over array and can transform each element and return new array

    filter()    create new array that satisfy given condition.

    reduce()    process elements of an array and return single value(can be number, string, object, array, etc.)

    slice()     creates shallow copy of portion of an array

    concat()    create new array by combining arrays/values

*/
    