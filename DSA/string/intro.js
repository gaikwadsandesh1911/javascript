/*  String

        A string is a sequence of characters used to represent text. 
        Characters are stored in contiguous memory locations and can be accessed using an index.

        In JavaScript, strings are immutable, which means you cannot change individual characters after the string is created.

            let str = 'america';
            str[0] = 'A';
            console.log(str);       // america

*/