/* 
    An array is a linear data structure 
    that stores a collection of elements of the same logical type 
    in contiguous memory locations, 
    where each element is identified by its index.

    In JavaScript, an array can technically store values of different types, 
    but in data structures and algorithms (DSA) and interview contexts, 
    arrays are generally treated as collections of the same datat type (e.g., all integers).
*/