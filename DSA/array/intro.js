/* 
    An array is a linear data structure 
    that stores a collection of elements in contiguous memory locations
    where each element is identified by its index.

    In JavaScript, an array can technically store values of different data types.

    But in the context of Data Structures and Algorithms (DSA), 
    arrays are generally treated as collections of the same data types ( e.g. all integers or all strings ).
*/